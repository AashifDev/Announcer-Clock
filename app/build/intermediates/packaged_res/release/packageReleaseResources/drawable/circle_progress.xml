<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <corners android:radius="12dp"/> <!-- corner radius -->
            <solid android:color="#E0E0E0"/> <!-- light gray background -->
        </shape>
    </item>

    <!-- Progress (filled part) -->
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <corners android:radius="12dp"/> <!-- same radius -->
                <solid android:color="#4CAF50"/> <!-- green progress -->
            </shape>
        </clip>
    </item>

    <!-- Secondary Progress (optional) -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape android:shape="rectangle">
                <corners android:radius="12dp"/>
                <solid android:color="#A5D6A7"/> <!-- lighter green -->
            </shape>
        </clip>
    </item>

</layer-list>
